/** Public logo path — replace `public/images/logo.png` without code changes. */
export const PORTAL_LOGO_PATH = "/images/logo.png";

/** Public favicon path — replace `public/favicon.ico` without code changes. */
export const PORTAL_FAVICON_PATH = "/favicon.ico";
